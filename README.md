
  # Maternal Health App Home Section

  This is a code bundle for Maternal Health App Home Section. 

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
